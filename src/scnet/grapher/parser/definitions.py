# -*- encoding: utf-8 -*-

from __future__ import unicode_literals

from scnet.grapher.tokenizer.definitions import token_types as lexer_token_types

class token_types(lexer_token_types):
    """
    Token Types which are being used during
    the parsing process.
    """
